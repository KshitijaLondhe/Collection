using System;
using System.Collections.Generic;
/*List<T>
It is same as arraylist, stored data in contiguous memory location
Similar type of objects / elements we can process
 Use of List<int> create & display  - 
 add,addrange,remove,removeat, clear ,sort, reverse…
*/

namespace Collection
{
    public class ListDemo
    {
       /* static void Main(string[] args)
        {
            List<int> list = new List<int>();
            list.Add(10);
            list.Add(50);
            list.Add(40);
            list.Add(30);
            list.Add(20);
            list.AddRange(list);
            list.Remove(10);
            list.RemoveRange(2, 2);
            list.RemoveAt(2);
            list.Reverse();
            list.Sort();
            list.Clear();
            foreach (int item in list)
            {
                Console.WriteLine(item);
            }

            //Use of List<Product>  create & display
            List<Product> list2 = new List<Product>();
            list2.Add(new Product("pen", 100));
            list2.Add(new Product("paper", 50));
            list2.Add(new Product("sharpner",60));

            foreach (Product item in list2)
            {
                Console.WriteLine(item);
            }

        }*/
    }
}
