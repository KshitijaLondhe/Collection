using System;
using System.Collections.Generic;

//Use dictionary<int,string>

namespace Collection
{
    public class DictionaryDemo
    {
       /* static void Main(string[] args)
        {
            Dictionary<int, string> dict = new Dictionary<int, string>();
            dict.Add(6, "White");
            dict.Add(8, "Red");
            dict.Add(1, "Black");
            dict.Add(3, "Green");
            dict.Remove(3);
           // dict.Reverse();
            Console.WriteLine(dict.ContainsKey(1));
            Console.WriteLine(dict.ContainsValue("Black"));
            Console.WriteLine("Elements in Dictionary :"+dict.Count);
            Console.WriteLine(dict.ElementAt(2));

            foreach (KeyValuePair<int, string> d in dict)
            {
                Console.WriteLine(d.Key+" "+d.Value);
            }
            foreach (var d1 in dict)
            {
                
                Console.WriteLine(d1.Key + " " + d1.Value);
            }
            foreach (var d1 in dict.Reverse())
            {

                Console.WriteLine(d1.Key + " " + d1.Value);
            }
        }
       */
    }
}
