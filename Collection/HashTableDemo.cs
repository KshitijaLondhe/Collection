using System;
using System.Collections;
/*Hashtable
It is used to store key-value pair data in hashtable
Key should be unique
Sort not possible, insert not possible
Internally Hashtable uses Hash algorithm to store data
*/

namespace Collection
{
    public class HashTableDemo
    {
        /*static void Main(string[] args)
        {
            Hashtable ht = new Hashtable();
            ht.Add(1, "kshitija");
            ht.Add(2, "pratiksha");
            ht.Add(3, "akshada");
            ht.Remove(2);
            // if match it will remove that object
    // DictionaryEntry is a built in struct type to read Key & Value from the Hashtable
            foreach (DictionaryEntry item in ht)
            {
                Console.WriteLine(item.Key + "  " + item.Value);
            }

        }*/
    }
}
