using System;
using System.Collections;
/*Stack Class→ 
Data will be added & removed using LIFO (Last in first out)
Can not insert or remove in between
Can not sort
Push - to add data
Pop - to remove
Pick - to select top element
*/

namespace Collection
{
    public class StackDemo
    {
        /*static void Main(string[] args)
        {
            Stack stack = new Stack();
            stack.Push(10);
            stack.Push(20.5);
            stack.Push("raj");
            stack.Push('k');
            stack.Push(20);
            stack.Pop();
            // remove the element at top
            Console.WriteLine("Top element " + stack.Peek());
            // select the top element


            foreach (var item in stack)
            {
                Console.WriteLine(item);
            }
        }*/
    }
}
