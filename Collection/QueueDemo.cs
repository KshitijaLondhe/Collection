using System;
using System.Collections;
/*Queue Class →
Data will be added & remove using FIFO (First in first out)
Can not insert or remove in between
Can not sort
Enqueue- to add
DeQueue - to remove
Pick - select the first element
*/

namespace Collection
{
    public class QueueDemo
    {
        /*static void Main(string[] args)
        {
            Queue que = new Queue();
            que.Enqueue(1);
            que.Enqueue(10.05);
            que.Enqueue("kshitija");
            que.Enqueue('r');
            que.Enqueue(20);
            que.Dequeue(); 
            // remove the first element
            Console.WriteLine("First element " + que.Peek());
            // select the first element



            foreach (var item in que)
            {
                Console.WriteLine(item);
            }
        }*/
    }
}
