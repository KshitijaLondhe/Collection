using System;
using System.Collections;
/*SortedList
It is used to stored key-value pair data
Data will be stored using key (asc order of key)
We can remove using key
Key should be unique
*/

namespace Collection
{
    public class SortedListDemo
    {
        /*static void Main(string[] args)
        {
            SortedList list = new SortedList();
            list.Add(3, "white");
            list.Add(1, "black");
            list.Add(4, "red");
            list.Add(2, "blue");
            list.Remove(4);
            list.RemoveAt(2);
            Console.WriteLine(list.ContainsValue("black"));
            foreach (DictionaryEntry item in list)
            {
                Console.WriteLine(item.Key+" "+item.Value);
            }
        }*/
    }
}
